
// includes
// devuelve valor true o false.
var mascotas = ['perro', 'gato', 'conejo'];
var resultado = mascotas.includes('gato');
console.log(resultado);

// Join
// si no recibe parametros, concatena los strings
var elementos = ['fuego', 'aire', 'agua'];
var result = elementos.join ()
console.log(result);

// los separo los string con un punto. No puede convertir un array de objetos a una concatenacion
var result2 = elementos.join ('.')
console.log(result2);

